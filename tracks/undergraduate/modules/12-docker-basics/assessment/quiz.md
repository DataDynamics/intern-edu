# 모듈 12 — 퀴즈 (Docker 기초)

## 개념
1. 이미지(image)와 컨테이너(container)의 차이는?
2. 볼륨(volume)은 왜 필요한가요? 없으면 어떤 문제가 생기나요?
3. `-p 5432:5432` 에서 두 숫자는 각각 무엇인가요?
4. `docker compose down` 과 `docker compose down -v` 의 차이는?
5. `/docker-entrypoint-initdb.d/` 에 둔 `.sql` 파일은 언제 실행되나요?

## 명령 작성
6. `postgres:16` 이미지를 백그라운드로 받아 실행 중인 컨테이너 목록을 보는 명령 2개를 쓰세요.
7. 실행 중인 컨테이너 `pg`의 로그를 보는 명령은?
8. 컨테이너 `pg` 안으로 들어가 bash 쉘을 여는 명령은?
9. compose로 띄운 db 서비스의 psql에 접속하는 명령은? (DB명 shop)
10. compose 파일 문법만 검증(데몬 없이)하는 명령은?
