# 모듈 12 — 완료 체크리스트

## 실습 통과 기준
- [ ] `examples/docker-compose.yml`로 Postgres를 띄우고 접속해봤다 (Docker 설치 시)
- [ ] `exercises/starter/docker-compose.yml`의 TODO를 모두 채웠다
- [ ] `docker compose config`(또는 `check.sh`)가 통과한다
- [ ] (설치 시) `products` 테이블을 조회해 3행을 확인했다
- [ ] `assessment/quiz.md`를 풀고 정답과 비교했다

## 역량 확인 (강사 구두 점검)
- [ ] 이미지/컨테이너/볼륨을 구분해 설명할 수 있다
- [ ] compose로 서비스를 정의하고 띄우는 흐름을 안다
- [ ] 포트 매핑과 볼륨 마운트의 의미를 안다
- [ ] init 스크립트 자동 실행 조건을 안다
