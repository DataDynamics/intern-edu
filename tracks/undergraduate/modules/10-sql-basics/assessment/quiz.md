# 모듈 10 — 퀴즈 (SQL 기초)

## 개념
1. 기본키(Primary Key)와 외래키(Foreign Key)의 차이는?
2. `WHERE`와 `HAVING`은 각각 언제 쓰나요?
3. `INNER JOIN`과 `LEFT JOIN`의 차이를 설명하세요.
4. SQL의 **실행** 순서에서 `FROM`, `WHERE`, `GROUP BY`, `SELECT` 의 순서는?
5. `SELECT DISTINCT city FROM customers` 는 무엇을 반환하나요?

## 쿼리 작성 (customers, orders 스키마 기준)
6. 'Busan'에 사는 고객의 이름을 조회하세요.
7. 'book' 카테고리 주문을 금액 높은 순으로 조회하세요.
8. 주문이 가장 비싼 1건의 금액을 조회하세요. (LIMIT 사용)
9. 각 고객의 이름과 그 고객의 주문 건수를 조회하세요. (JOIN + GROUP BY)
10. 도시(city)별 고객 수를 많은 순으로 조회하세요.
