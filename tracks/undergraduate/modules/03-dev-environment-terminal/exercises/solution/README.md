# 정답 해설 — 환경 점검 스크립트

- `platform.python_version()` 으로 버전 문자열을 얻는다
- 버전 비교는 `"3.12.3".split(".")` → `["3","12","3"]` 를 정수로 변환해 판단
- `greet`은 f-string으로 문자열 조합

이 작은 스크립트가 동작한다면, **가상환경 + Python 실행 환경이 정상**이라는 뜻입니다.
이제 모듈 05부터 본격적인 학습을 시작할 준비가 됐습니다.
