# 모듈 06 — 퀴즈 (Python 기초 1)

## 개념
1. 다음 값들의 자료형을 쓰세요: `10`, `3.14`, `"hi"`, `True`
2. `10 / 3`, `10 // 3`, `10 % 3` 의 결과를 각각 쓰세요.
3. f-string이란 무엇이며, `name="logs"` 일 때 `f"{name}.txt"` 의 결과는?
4. `if / elif / else`에서 들여쓰기가 왜 중요한가요?
5. `break`와 `continue`의 차이를 설명하세요.

## 코드 읽기
6. 다음 코드의 출력은?
   ```python
   total = 0
   for n in [1, 2, 3, 4]:
       if n % 2 == 0:
           total += n
   print(total)
   ```
7. 다음 코드의 출력은?
   ```python
   for i in range(3):
       for j in range(2):
           print(i, j)
   ```

## 코드 작성
8. 점수(`score`)가 90 이상이면 "A", 80 이상이면 "B", 그 외 "C"를 출력하는 코드를 쓰세요.
9. 리스트 `nums = [5, 3, 8, 1]`의 **최댓값**을 반복문으로 구하세요. (`max()` 사용 금지)
10. 1부터 100까지 정수 중 3의 배수의 합을 구하는 코드를 쓰세요.
