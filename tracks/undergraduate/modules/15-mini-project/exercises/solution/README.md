# 참고 구현 — 미니 프로젝트

`pipeline.py`가 load → clean → analyze → save 전 과정을 담은 참고 구현입니다.
정제 규칙(공백/대소문자/결측/중복)과 분석(카테고리·날짜·결제수단 집계)을 보여줍니다.
`REPORT.md`는 발표용 리포트 예시입니다.

직접 풀 때는 정제 규칙을 스스로 정하고, 그 **판단 근거**를 REPORT에 남기는 것이 핵심입니다.
