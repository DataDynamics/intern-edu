# 모듈 14 — 퀴즈 (EDA & 시각화)

## 개념
1. EDA(탐색적 데이터 분석)란 무엇이며 왜 먼저 하나요?
2. `df.describe()` 가 알려주는 통계 3가지를 쓰세요.
3. 평균(mean)과 중앙값(median)이 크게 다르면 무엇을 의심할 수 있나요?
4. 서버(화면 없는 환경)에서 차트를 만들 때 `plt.show()` 대신 무엇을 쓰나요?
5. 막대그래프와 히스토그램은 각각 언제 쓰나요?

## 코드 작성 (df: category, amount)
6. 카테고리별 개수를 세는 코드를 쓰세요. (`value_counts`)
7. amount의 분포를 히스토그램으로 그리는 코드를 쓰세요.
8. 차트를 화면 없이 `chart.png`로 저장하는 코드(백엔드 설정 포함)를 쓰세요.
9. amount의 25%, 50%, 75% 분위수를 구하는 코드를 쓰세요.

## 해석
10. "평균 주문액 13,062, 중앙값 10,500, 최대 30,000" 이 데이터에서 읽을 수 있는 사실을 한 문장으로 쓰세요.
