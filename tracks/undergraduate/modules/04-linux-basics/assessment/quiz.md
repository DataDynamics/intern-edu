# 모듈 04 — 퀴즈 (Linux 기초)

스스로 답을 적어본 뒤 `quiz_answers.md`와 비교하세요.

## 개념
1. 절대경로와 상대경로의 차이는 무엇인가요? 각각 예를 하나씩 드세요.
2. `.`, `..`, `~` 는 각각 무엇을 의미하나요?
3. `>` 와 `>>` 의 차이를 설명하세요.
4. `uniq` 앞에 항상 `sort`를 붙이는 이유는 무엇인가요?
5. 권한 `rwxr-xr--` 를 숫자(3자리)로 표현하면?

## 명령어 작성
다음을 수행하는 명령(또는 파이프라인)을 작성하세요. (`access.log` 기준)
6. `app.log`에서 "ERROR"가 포함된 줄의 **개수**만 출력
7. 현재 폴더와 하위 폴더에서 확장자가 `.csv`인 파일 모두 찾기
8. `access.log`에서 상태코드(9번째 필드) 빈도를 많은 순으로 출력
9. `script.sh`에 실행 권한을 주고 실행하기 (2줄)
10. `names.txt`를 정렬해 중복을 제거한 결과를 `unique.txt`로 저장

## 응용
11. 쉘 스크립트 첫 줄 `#!/bin/bash` 를 무엇이라 부르며, 왜 필요한가요?
12. 현재 폴더의 모든 `.csv` 파일에 대해 줄 수를 출력하는 `for` 반복문을 작성하세요.
